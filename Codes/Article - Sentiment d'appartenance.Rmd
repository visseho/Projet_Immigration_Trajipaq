---
title: "Article - Sentiment d'appartenance"
author: "Thea"
date: "2023-05-17"
output: pdf_document
---

```{r}
library(summarytools)
library(tidyverse)
library(ggthemes)
library(haven)
```

```{r}
Base_Trajipaq_Finale_Ok <- read_dta("~/Desktop/Base_Trajipaq_Finale_Ok.dta")
```

# Question de recherche : de quelles façons les discriminations que subissent les personnes immigrantes influencent leur sentiment d'appartenance au Québec ?

## Variable indépendante = Les discriminations que subissent les personnes immigrantes :

- dans l'emploi
- dans la recherche de logement
- dans la santé
- sur le lieu de formation

Type de variable = factoriel
Niveau de mesure = ordinal (échelle de Likert)

Réponses possibles :

01. Tout à fait d'accord
02. Plutôt d'accord
03. Plutôt pas d'accord
04. Pas du tout d'accord
98. Je ne sais pas
99. Je ne préfère pas répondre

## Variable dépendante = le sentiment d'appartenance au Québec :

- se sentir chez soi au Québec
- la nécessité d'oublier sa culture d'origine pour s'intégrer au Québec
- être vu(e) comme un(e) Québécois(e)
- se sentir Québécois(e)
- se sentir Canadien(ne)
- avoir un sentiment d'appartenance avec son pays d'origine

Type de variable = factoriel
Niveau de mesure = ordinal (échelle de Likert)

Réponses possibles :

01. Tout à fait d'accord
02. Plutôt d'accord
03. Plutôt pas d'accord
04. Pas du tout d'accord
98. Je ne sais pas
99. Je ne préfère pas répondre

## Variables de contrôle = caractéristiques sociodémographiques :

- sexe
- statut matrimonial
- pays d'origine
- année d'obtention de la résidence permanente
- année d'obtention de la citoyenneté canadienne
- année d'arrivée au Québec
- année depuis la première installation au Québec
- statut d'immigration avant l'obtention de la résidence permanente
- niveau d'éducation
- religion
- importance accordée à la religion
- compétence : lire le français
- compétence : parler français
- compétence : rédiger en français
- compétence : comprendre le français
- niveau de connaissance en français lors de l'arrivée au Québec
- compétence : lire l'anglais
- compétence : parler anglais
- compétence : rédiger en anglais
- compétence : comprendre l'anglais
- niveau de connaissance en anglais lors de l'arrivée au Québec
- suivi de cours dédiés à la francisation après l'arrivée au Québec
- état de santé
- problèmes de santé avant l'installation au Québec
- problèmes de santé au cours des 12 derniers mois
- problèmes de santé il y a moins de 5 ans
- problèmes de santé il y a plus de 10 ans
- lieu de naissance de la mère
- niveau d'éducation de la mère
- lieu de naissance du père
- niveau d'éducation du père

# Présentation des variables de contrôle

## Q10. Êtes-vous un homme, une femme ou autre ?

01. Homme
02. Femme
97. Autre

Type de variable = catégorielle
Niveau de mesure = nominal

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q10_F = case_when(
    Q10 == 1 ~ "Homme",
    Q10 == 2 ~ "Femme",
    Q10 == 97 ~ "Autre"
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q10_F)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q10_F)) +
  geom_bar(fill = "pink") +
  theme_bw()

```

Dans le tableau de fréquence et le diagramme de barres ci-dessus, on observe que le sexe modal est "Femme".

## STATUT. Êtes-vous actuellement... ?

01. Célibataire/jamais marié(e) ou conjoint(e) de fait
02. Marié(e)
03. Conjoint(e) de fait/en union libre
04. Séparé(e)
05. Divorcé(e)
06. Veuf(ve)

Type de variable = catégorielle
Niveau de mesure = nominal

```{r}

freq(Base_Trajipaq_Finale_Ok$STATUT)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = STATUT)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le tableau de fréquence et le diagramme de barres ci-dessus, on observe que le statut modal est "Marié(e)".

## Q11. Dans quel pays êtes-vous né ?

Pays. (liste déroulante)
998. Je ne sais pas
999. Je préfère ne pas répondre

Type de variable = catégorielle
Niveau de mesure = nominal

```{r}

freq(Base_Trajipaq_Finale_Ok$Q11)

```

```{r}
ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = as.factor(Q11))) +
  geom_bar(fill = "pink") +
  theme_bw()
```

Dans le tableau de fréquence et le diagramme de barres ci-dessous, on peut observer que les pays d'origine modaux sont la France (26.685%) et l'Algérie (6.649%).

## Q12. En quelle année avez-vous obtenu la résidence permanente ?

Année
9998. Je ne sais pas
9999. Je préfère ne pas répondre

Type de variable = catégorielle
Niveau de mesure = nominale

## Q13. En quelle année est-ce que vous êtes devenu citoyen(ne) ?

Année
9998. Je ne sais pas
9999. Je préfère ne pas répondre

Type de variable = numérique continue
Niveau de mesure = ratio


## Q14I. Depuis quelle année vivez-vous au Québec ?

Année
9996. La même année que la résidence permanente
9998. Je ne sais pas
9999. Je préfère ne pas répondre

Type de variable = numérique continue
Niveau de mesure = ratio

## Q14N. En quelle année avez-vous commencé à vivre au Québec pendant au moins un an ?

Année
9996. Depuis la naissance
9998. Je ne sais pas
9999. Je préfère ne pas répondre

Type de variable = numérique continue
Niveau de mesure = ratio

## Q15I. Avant l’obtention de la résidence permanente, quel était votre statut d’immigration ?

1. Travailleur temporaire
02. Demandeur d’asile
03. Étudiant étranger
04. Touriste
97. Autre, préciser
98. Je ne sais pas
99. Je préfère ne pas répondre

Type de variable = catégorielle
Niveau de mesure = nominal

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q15M1 = case_when(
    Q15M1 == 1 ~ 1,
    Q15M1 == 2 ~ 2,
    Q15M1 == 3 ~ 3,
    Q15M1 == 4 ~ 4,
    Q15M1 == 97 ~ 5,
    Q15M1 == 98 ~ NA,
    Q15M1 == 99 ~ NA
  ))

```


```{r}

freq(Base_Trajipaq_Finale_Ok$Q15M1)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q15M1)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le tableau de fréquence et le diagramme de barres ci-dessus, on observe que la réponse modale est "travailleur temporaire".

## Q16. Quel est votre plus haut diplôme d’études obtenu ?

01. Moins que le secondaire
02. Secondaire ou équivalent
03. Collégial
04. Formation professionnelle ou post-secondaire
05. Université 1er cycle
06. Université 2ème cycle et plus
98. Je ne sais pas
99. Je préfère ne pas répondre

Type de variable = factorielle
Niveau de mesure = ordinal

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q16 = case_when(
    Q16 == 1 ~ 1,
    Q16 == 2 ~ 2,
    Q16 == 3 ~ 3,
    Q16 == 4 ~ 4,
    Q16 == 5 ~ 5,
    Q16 == 6 ~ 6,
    Q16 == 98 ~ NA,
    Q16 == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q16)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q16)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le tableau de fréquence et le diagramme de barres ci-dessus, on observe que la distribution du niveau d'éducation est asymétrique vers la gauche. Autrement dit, l'échantillon est composé à 67.31% de personnes titulaires d'un diplôme universitaire.

## Q17. Actuellement, avez-vous une religion ?

00. Sans religion
01. Catholique
02. Protestante
03. Évangéliste
04. Mormone
05. Témoins de Jéhovah
06. Orthodoxe
07. Autres Chrétiens
08. Musulmans
09. Juif
10. Bouddhiste
11. Hindou
12. Animiste
97. Autre
98. Je ne sais pas
99. Je préfère ne pas répondre

Type de variable = catégorielle
Niveau de mesure = nominal

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q17 = case_when(
    Q17 == 0 ~ 0,
    Q17 == 1 ~ 1,
    Q17 == 2 ~ 2,
    Q17 == 3 ~ 3,
    Q17 == 4 ~ 4,
    Q17 == 5 ~ 5,
    Q17 == 6 ~ 6,
    Q17 == 7 ~ 7,
    Q17 == 8 ~ 8,
    Q17 == 9 ~ 9,
    Q17 == 10 ~ 10,
    Q17 == 11 ~ 11,
    Q17 == 12 ~ 12,
    Q17 == 97 ~ 13,
    Q17 == 98 ~ NA,
    Q17 == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q17)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q17)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Le tableau de fréquence et le diagramme de barres ci-dessus nous montrent que les réponses modales sont "sans religion" et "catholique". On remarque également un nombre remarquable de réponses "orthodoxe" et "musulman".

## Q18. Quelle importance accordez-vous aujourd’hui à la religion dans votre vie ?

01. Pas du tout d’importance
02. Un peu d’importance
03. Assez d’importance
04. Beaucoup d’importance
98. Je ne sais pas
99. Je préfère ne pas répondre

Type de variable = factoriel
Niveau de mesure = ordinal

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q18 = case_when(
    Q18 == 1 ~ 1,
    Q18 == 2 ~ 2,
    Q18 == 3 ~ 3,
    Q18 == 4 ~ 4,
    Q18 == 98 ~ NA,
    Q18 == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q18)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q18)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Sur le tableau de fréquence et le diagramme de barres ci-dessus, on observe que la distribution est asymétrique sur la droite. Autrement dit, 51.7% des répondants disent que la religion occupe une place dans leur vie. Néanmoins, le nombre élevé de réponse "je ne sais pas" (39.8%) réduit la validité de cette question.

## Q20. Quand vous étiez enfant, quelles étaient la langue ou les langues que vous parliez le plus souvent, à l’âge de 5 ans et que vous comprenez toujours? (Plusieurs réponses possibles).

01. Français
02. Anglais
97. Autre : préciser, possibilité d’en déclarer deux
98. Je ne sais pas
99. Je préfère ne pas répondre

Type de variable = catégoriel
Niveau de mesure = nominal

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q20M1 = case_when(
    Q20M1 == 1 ~ 1,
    Q20M1 == 2 ~ 2,
    Q20M1 == 97 ~ 3,
    Q20M1 == 98 ~ NA,
    Q20M1 == 99 ~ NA
  ))

```

```{r}
freq(Base_Trajipaq_Finale_Ok$Q20M1)
```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q20M1)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le tableau de fréquence et le diagramme de barres ci-dessus, on observe que la réponse modale est "français". Autrement dit, le français constitue la langue maternelle de 63.84% des répondants.

## I3. Score de compétence linguistique (français)

Type de variable = factoriel
Niveau de mesure = ordinal

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q21A = case_when(
    Q21A == 1 ~ 1,
    Q21A == 2 ~ 2,
    Q21A == 3 ~ 3,
    Q21A == 4 ~ 4,
    Q21A == 98 ~ NA,
    Q21A == 99 ~ NA
  ))

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q21B = case_when(
    Q21B == 1 ~ 1,
    Q21B == 2 ~ 2,
    Q21B == 3 ~ 3,
    Q21B == 4 ~ 4,
    Q21B == 98 ~ NA,
    Q21B == 99 ~ NA
  ))

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q21C = case_when(
    Q21C == 1 ~ 1,
    Q21C == 2 ~ 2,
    Q21C == 3 ~ 3,
    Q21C == 4 ~ 4,
    Q21C == 98 ~ NA,
    Q21C == 99 ~ NA
  ))

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q21D = case_when(
    Q21D == 1 ~ 1,
    Q21D == 2 ~ 2,
    Q21D == 3 ~ 3,
    Q21D == 4 ~ 4,
    Q21D == 98 ~ NA,
    Q21D == 99 ~ NA
  ))

```

```{r}

v_I3 <- c("Q21A",
          "Q21B",
          "Q21C",
          "Q21D"
          )

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  rowwise() %>%
  mutate(I3 = (sum(c_across(all_of(v_I3)), na.rm = TRUE) - length(v_I3)) / (3*length(v_I3))) %>%
  mutate(I3 = if (I3 < 0) NA_real_ else I3) %>%
  ungroup()

```

```{r}

descr(Base_Trajipaq_Finale_Ok$I3)

```


```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = I3)) +
  geom_histogram(na.rm = TRUE, fill = "pink", binwidth = 0.01) +
  theme_bw()

```


# Présentation de la variable indépendante

## Q39A. Dites si vous êtes tout à fait d’accord, plutôt d’accord, plutôt pas d’accord ou pas du tout d’accord avec les propositions suivantes (participation identitaire et sentiment d’appartenance) : je me sens chez moi au Québec.

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q39A = case_when(
    Q39A == 1 ~ 4,
    Q39A == 2 ~ 3,
    Q39A == 3 ~ 2,
    Q39A == 4 ~ 1,
    Q39A == 98 ~ NA,
    Q39A == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q39A)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la gauche. Autrement dit, 87.01% des personnes interrogées sont tout à fait d'accord ou plutôt d'accord pour dire qu'elles se sentent chez elles au Québec. On remarque également que 1.79% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q39A)) +
  geom_bar(fill = "pink",
           na.rm = TRUE
           ) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la gauche mentionnée précédemment.

## Q39B. Dites si vous êtes tout à fait d’accord, plutôt d’accord, plutôt pas d’accord ou pas du tout d’accord avec les propositions suivantes (participation identitaire et sentiment d’appartenance) : il faut faire oublier ses origines pour se faire accepter au Québec.

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q39B = case_when(
    Q39B == 1 ~ 4,
    Q39B == 2 ~ 3,
    Q39B == 3 ~ 2,
    Q39B == 4 ~ 1,
    Q39B == 98 ~ NA,
    Q39B == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q39B)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la droite. Autrement dit, 67.88% des personnes interrogées ne sont pas du tout d'accord ou plutôt pas d'accord pour dire qu'il faut faire oublier ses origines pour se faire accepter au Québec. On remarque également que 3.96% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q39B)) +
  geom_bar(fill = "pink",
           na.rm = TRUE
           ) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la droite mentionnée précédemment.

## Q39C. Dites si vous êtes tout à fait d’accord, plutôt d’accord, plutôt pas d’accord ou pas du tout d’accord avec les propositions suivantes (participation identitaire et sentiment d’appartenance) : on me voit comme une Québécoise.

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q39C = case_when(
    Q39C == 1 ~ 4,
    Q39C == 2 ~ 3,
    Q39C == 3 ~ 2,
    Q39C == 4 ~ 1,
    Q39C == 98 ~ NA,
    Q39C == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q39C)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est bimodale (1 et 4). Autrement dit, cette question divise nos répondants. On remarque également que 5.63% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q39C)) +
  geom_bar(fill = "pink",
           na.rm = TRUE
           ) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe les deux modes de la distribution décrits précédemment.

## Q39D. Dites si vous êtes tout à fait d’accord, plutôt d’accord, plutôt pas d’accord ou pas du tout d’accord avec les propositions suivantes (participation identitaire et sentiment d’appartenance) : je me sens Québécois(e).

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q39D = case_when(
    Q39D == 1 ~ 4,
    Q39D == 2 ~ 3,
    Q39D == 3 ~ 2,
    Q39D == 4 ~ 1,
    Q39D == 98 ~ NA,
    Q39D == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q39D)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la gauche. Autrement dit, 60.72% des personnes interrogées sont tout à fait d'accord ou plutôt d'accord pour dire qu'elles se sentent Québécois(e)s. On remarque également que 4.03% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q39D)) +
  geom_bar(fill = "pink",
           na.rm = TRUE
           ) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la gauche mentionnée précédemment.

## Q39E. Dites si vous êtes tout à fait d’accord, plutôt d’accord, plutôt pas d’accord ou pas du tout d’accord avec les propositions suivantes (participation identitaire et sentiment d’appartenance) : je me sens Canadien(ne).

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q39E = case_when(
    Q39E == 1 ~ 4,
    Q39E == 2 ~ 3,
    Q39E == 3 ~ 2,
    Q39E == 4 ~ 1,
    Q39E == 98 ~ NA,
    Q39E == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q39E)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la gauche. Autrement dit, 79.98% des personnes interrogées sont tout à fait d'accord ou plutôt d'accord pour dire qu'elles se sentent Canadien(ne). On remarque également que 2.69% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q39E)) +
  geom_bar(fill = "pink",
           na.rm = TRUE
           ) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la gauche mentionnée précédemment.

## Q39F. Dites si vous êtes tout à fait d’accord, plutôt d’accord, plutôt pas d’accord ou pas du tout d’accord avec les propositions suivantes (participation identitaire et sentiment d’appartenance) : j'ai un sentiment d'appartenance à mon pays d'origine.

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q39F = case_when(
    Q39F == 1 ~ 4,
    Q39F == 2 ~ 3,
    Q39F == 3 ~ 2,
    Q39F == 4 ~ 1,
    Q39F == 98 ~ NA,
    Q39F == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q39F)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la gauche. Autrement dit, 81.42% des personnes interrogées sont tout à fait d'accord ou plutôt d'accord pour dire qu'elles possèdent un sentiment d'appartenance envers leur pays d'origine. On remarque également que 2.69% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q39F)) +
  geom_bar(fill = "pink",
           na.rm = TRUE
           ) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la gauche mentionnée précédemment.

## I1 : L'indice du sentiment d'appartenance au Québec

A la différence des variables précédentes, l'indice du sentiment d'appartenance est une variable numérique continue dont le niveau de mesure est ratio.

```{r}

v_I1 <- c("Q39A",
          "Q39B",
          "Q39C",
          "Q39D",
          "Q39E",
          "Q39F"
          )

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  rowwise() %>%
  mutate(I1 = (sum(c_across(all_of(v_I1)), na.rm = TRUE) - length(v_I1)) / (3*length(v_I1))) %>%
  mutate(I1 = if (I1 < 0) NA_real_ else I1) %>%
  ungroup()

```

```{r}

descr(Base_Trajipaq_Finale_Ok$I1)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = I1)) +
  geom_histogram(na.rm = TRUE, fill = "pink", binwidth = 0.01) +
  theme_bw()

```

Le tableau descriptif et l'histogramme ci-dessous nous indiquent que la distribution est asymétrique vers la gauche. Autrement dit, les répondants estiment en moyenne entretenir un sentiment d'appartenance relativement développé (0.59).

# Présentation de la variable dépendante

## Q40A. Au cours des 12 mois (avant la crise de la COVID-19), pensez-vous avoir subi des traitements inégalitaires ou des discriminations ? Un traitement inégalitaire ou une discrimination se repère si « vous avez été moins bien traité que les autres, sans raison valable » : dans l'emploi ?

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q40A = case_when(
    Q40A == 1 ~ 3,
    Q40A == 2 ~ 2,
    Q40A == 3 ~ 1,
    Q40A == 96 ~ NA,
    Q40A == 98 ~ NA,
    Q40A == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q40A)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la droite Autrement dit, 65.44% des personnes interrogées disent ne jamais avoir subit de discrimination dans l'emploi au cours des 12 derniers mois précédent la crise du covid 19. On remarque également que 9.47% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q40A)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la droite mentionnée précédemment.

## Q40B. Au cours des 12 mois (avant la crise de la COVID-19), pensez-vous avoir subi des traitements inégalitaires ou des discriminations ? Un traitement inégalitaire ou une discrimination se repère si « vous avez été moins bien traité que les autres, sans raison valable » : dans la recherche de logement ?

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q40B = case_when(
    Q40B == 1 ~ 3,
    Q40B == 2 ~ 2,
    Q40B == 3 ~ 1,
    Q40B == 96 ~ NA,
    Q40B == 98 ~ NA,
    Q40B == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q40B)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la droite Autrement dit, 79.64% des personnes interrogées disent ne jamais avoir subit de discrimination dans la recherche de logement au cours des 12 derniers mois précédent la crise du covid 19. On remarque également que 17.34% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q40B)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la droite mentionnée précédemment.

## Q40C. Au cours des 12 mois (avant la crise de la COVID-19), pensez-vous avoir subi des traitements inégalitaires ou des discriminations ? Un traitement inégalitaire ou une discrimination se repère si « vous avez été moins bien traité que les autres, sans raison valable » : dans la santé ?

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q40C = case_when(
    Q40C == 1 ~ 3,
    Q40C == 2 ~ 2,
    Q40C == 3 ~ 1,
    Q40C == 96 ~ NA,
    Q40C == 98 ~ NA,
    Q40C == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q40C)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la droite Autrement dit, 82.26% des personnes interrogées disent ne jamais avoir subit de discrimination dans la santé au cours des 12 derniers mois précédent la crise du covid 19. On remarque également que 10.17% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q40C)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la droite mentionnée précédemment.

## Q40D. Au cours des 12 mois (avant la crise de la COVID-19), pensez-vous avoir subi des traitements inégalitaires ou des discriminations ? Un traitement inégalitaire ou une discrimination se repère si « vous avez été moins bien traité que les autres, sans raison valable » : sur le lieu de formation ?

```{r}

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  mutate(Q40D = case_when(
    Q40D == 1 ~ 3,
    Q40D == 2 ~ 2,
    Q40D == 3 ~ 1,
    Q40D == 96 ~ NA,
    Q40D == 98 ~ NA,
    Q40D == 99 ~ NA
  ))

```

```{r}

freq(Base_Trajipaq_Finale_Ok$Q40D)

```

Dans le tableau de fréquence ci-dessus, on observe que la distribution est asymétrique vers la droite Autrement dit, 81.40% des personnes interrogées disent ne jamais avoir subit de discrimination sur le lieu de formation au cours des 12 derniers mois précédent la crise du covid 19. On remarque également que 15.74% des personnes interrogées ne savaient pas ou n'ont pas souhaité répondre à la question. Etant donné la faible proportion de ces réponses, celles-ci seront retirées de l'analyse.

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = Q40D)) +
  geom_bar(fill = "pink", na.rm = TRUE) +
  theme_bw()

```

Dans le diagramme de barres ci-dessus, on observe l'asymétrie vers la droite mentionnée précédemment.

## I2 : L'indice de discrimination lors des 12 derniers mois précédent la crise du covid 19

A la différence des variables précédentes, l'indice du sentiment d'appartenance est une variable numérique continue dont le niveau de mesure est ratio.

```{r}

v_I2 <- c("Q40A",
          "Q40B",
          "Q40C",
          "Q39D"
          )

Base_Trajipaq_Finale_Ok <-
  Base_Trajipaq_Finale_Ok %>%
  rowwise() %>%
  mutate(I2 = (sum(c_across(all_of(v_I2)), na.rm = TRUE) - length(v_I2)) / (2.25*length(v_I2))) %>%
  mutate(I2 = if (I2 < 0) NA_real_ else I2) %>%
  ungroup()

```

```{r}

descr(Base_Trajipaq_Finale_Ok$I2)

```

```{r}

ggplot(data = Base_Trajipaq_Finale_Ok, mapping = aes(x = I2)) +
  geom_histogram(na.rm = TRUE, fill = "pink", binwidth = 0.01) +
  theme_bw()

```

Le tableau descriptif et l'histogramme ci-dessous nous indiquent que la distribution est asymétrique vers la droite. Autrement dit, les répondants estiment en moyenne avoir subit une faible discrimination (0.28). Lorsque l'on regarde la distribution de chaque question composant l'indice, le mode correspond à l'absence de discrimination (1). Or, l'indice présente une moyenne non nulle (0.28 > 0), suggérant que peu de répondants ne subissent aucune discrimination.